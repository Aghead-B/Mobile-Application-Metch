import 'package:flutter/material.dart';

class MyMatchPage extends StatelessWidget {
  const MyMatchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
